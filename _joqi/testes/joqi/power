from	pessoas,
	inteiros,
	strings,
	floats,
	todos,
	codigos
where	pessoas.cdPessoa = inteiros.intValue and
	inteiros.toString = strings.toString and
        strings.toString = floats.toString and
        floats.toString = todos.toString and
	todos.toString = codigos.toString